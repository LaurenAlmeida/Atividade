  <div class="container">
<table class="table">
    <tr>
        <th>Título</th>
        <th>Autor</th>
    </tr>

    <% @books.each do |book| %>
        <tr>
            <td><%= book.title %></td>
            <td><%= book.author %></td>
            <td><%= link_to "Edit", edit_book_path(book.id) %></td>
<td><%= link_to "Destroy", book_path(book.id), data: {turbo_method: :delete, turbo_confirm: "Tem certeza?"} %></td>
        </tr>
    <% end %>
</table>
<h5><%= link_to "Cadastre um novo livro", new_book_path %></h5>
</div>